# Operations

Use `make apply` after policy changes and `make test` before work. Publish local-only services using SSH `LocalForward`. Export work with Git bundles, rotate sandbox credentials after every lifecycle, retain Squid logs as required, and recreate rather than repair a suspected compromised VM.
